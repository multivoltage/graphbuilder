//import graphbuilder from './buildersgraphbuilder.js';

const graphutils = {

    /**
     * return an array of nodes ids
     */
    calcMinPath(src,dst){
        if(!src || !dst){
            console.log('cannot calculate min path from undefined src/dst nodes');
            return [];
        }

        let steps = [];

        return steps;
    }

  
};
export default graphutils;
